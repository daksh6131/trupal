# Disable symlinks which are causing EEXIST errors
symlink=false
# Use copy instead of symlinks
bin-links=false
# Ensure we're not using global packages
global=false
# Increase network timeout for better reliability
network-timeout=60000
